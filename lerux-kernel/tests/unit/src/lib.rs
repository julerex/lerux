//! Host unit tests for kernel-native logic that can run without the custom kernel target.
//!
//! See `README.md` for what belongs here vs `validation/trampolines/` vs `#[cfg(test)]` in src.

mod common;
