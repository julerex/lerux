[package]
name = "lerux-fs"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "On-disk filesystem format for lerux (LERUXFS2)"

[lints]
workspace = true

[dependencies]
lerux-interface-types = { path = "../lerux-interface-types" }