[package]
name = "lerux-proc"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
redox-rt = { path = "../runtime/redox-rt" }
redox_syscall = { version = "0.8.0", default-features = false }

[lints]
workspace = true
